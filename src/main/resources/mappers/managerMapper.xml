<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.good.mapper.managerMapper">

	<select id="login" resultType="ManagerVO">
        select * from manager where adminID = #{adminID} and adminPWD = #{adminPWD}
    </select>
    
    <update id="authorUp">
    	UPDATE accounts SET authority = #{authority} WHERE accountid = #{accountid}
    </update>

</mapper>


